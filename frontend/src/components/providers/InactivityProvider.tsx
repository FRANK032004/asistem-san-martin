'use client';

/**
 * Provider que gestiona la inactividad del usuario
 * 
 * NOTA: DESHABILITADO TEMPORALMENTE PARA PRUEBAS
 */
export function InactivityProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export default InactivityProvider;
